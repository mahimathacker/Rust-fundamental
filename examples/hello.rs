// cargo run --example hello

//Attributes  - metadata for the compiler
#![allow(unused)] //This will not compile unused variables and data for warning

//main: entry point
fn main() {
    //Macros :  generate code at compile time are invoked with the exclamation mark (!).

    println!("Hello, world!");
}
